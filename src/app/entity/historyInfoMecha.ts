export interface historyInfoMecha {
  // 履歴ID
  historyId: string;
  // 伝票番号
  slipNo: string;
  // 伝票タイトル
  slipTitle: string;
  // メカニックID
  mechanicId: string;
  // 表示区分
  completionDate: string;
  // 表示区分
  displayDiv: string;
  // 作成日時
  created: string;
  // 更新日時
  updated: string;
}
