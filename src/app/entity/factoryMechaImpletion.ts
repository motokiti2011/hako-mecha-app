export interface factoryMechaImpletion {
  // ID
  id: string;
  // 工場メカニックID
  factoryMechanicId: string;
  // サービスタイプ
  serviceType: string;
  // お気に入り数
  favoriteCount: string;
  // 過去の取引件数
  transactionHistoryCount: string;
  // 作成日時
  created: string;
  // 更新日時
  updated: string;
}
