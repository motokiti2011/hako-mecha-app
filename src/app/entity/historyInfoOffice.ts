export interface historyInfoOffice {
  // 履歴ID
  historyId: string;
  // 伝票番号
  slipNo: string;
  // 伝票タイトル
  slipTitle: string;
  // 工場ID
  officeId: string;
  // メカニックID
  mechanicId: string;
  // 完了日
  completionDate: string;
  // 表示区分
  displayDiv: string;
  // 作成日時
  created: string;
  // 更新日時
  updated: string;
}
