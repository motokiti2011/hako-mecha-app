export interface historyUserInfo {
  // 履歴ID
  historyId: string;
  // ユーザーID
  userId: string;
  // 車両ID
  vehicleId: string;
  // 伝票ID
  slipNo: string;
  // サービスタイトル
  slipTitle: string;
  // 事業所ID
  officeId: string;
  // メカニックID
  mechanicId: string;
  // 完了日
  completionDate: string;
  // 表示区分
  displayDiv: string;
  // 作成日時
  created: string;
  // 更新日時
  updated: string;
}
