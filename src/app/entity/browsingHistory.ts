export interface browsingHistory {
  // ID
  id: string;
  // ユーザーID
  userId: string;
  // 伝票番号
  slipNo: string;
  // タイトル
  title: string;
  // 価格
  price: string;
  // 期間
  whet: string;
  // 終了日
  endDate: string;
  // 画像url
  imageUrl:string;
  // サービスタイプ
  serviceType: string;
  // 作成日時
  created: string;
  // 更新日時
  updated: string;
}

