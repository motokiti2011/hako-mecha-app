export interface factoryMechanicFavorite {
  // ID
  id: string;
  // ユーザーID
  userId: string;
  // サービスタイプ
  serviceType: string;
  // お気に入り対象ID
  favoriteId: string;
  // お気に入り対象名
  favoriteName: string;
  // 作成日時
  created: string;
  // 更新日時
  updated: string;
}
